---
const { class: titleClass, lineClass } = Astro.props;
---

<div class="flex flex-col items-center gap-3 pb-4 sm:pb-6 md:pb-12">
  <h2
    class:list={[
      "text-primary text-center text-3xl font-bold sm:text-4xl md:text-5xl",
      titleClass,
    ]}
  >
    <slot />
  </h2>
  <div class:list={["bg-primary h-1 w-20", lineClass]}></div>
</div>
